FLASK_APP=rotk.py

FLASK_DEBUG=1

# Secret key for Flask session / WTForms CSRF. Set to a long random string.
SECRET_KEY=''

# MySQL root password (used to create the DB and the app user on first boot).
MYSQL_ROOT_PASSWORD=''

# Password for the rotk_app application user (least-privileged DML only).
MYSQL_APP_PASSWORD=''

CERTFILE=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
KEYFILE=/etc/letsencrypt/live/yourdomain.com/privkey.pem
